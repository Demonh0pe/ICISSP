# 扩展版论文实验计划

> 面向 AutoDL RTX 5090,三天窗口,按量计费。
> 在实例上 `git pull` 即可查看最新版。

---

## 0. 先说清楚:哪些是事实,哪些是我的推断

我没有看过你的任何原始文件(`train_swap.py`、原 notebook、结果 CSV、会议版论文都没有)。下面标注了来源:

| 内容 | 来源 |
|---|---|
| 七个方法含 casr / hybrid / olora / replay | 你的交接文字 |
| 指标是 sklearn binary f1(非严格 Macro-F1) | 你的交接文字 |
| 会议版 p=0.026 是已发表事实,不动 | 你的交接文字 |
| 原实验在 A100,新实验在 5090 | 你的交接文字 |
| 两条扩展轴:换模型(Qwen)、换数据集(CVEfixes) | 你的交接文字 |
| **课题是"LLM 漏洞检测中的持续学习/灾难性遗忘"** | **我的推断** —— olora 是 O-LoRA(正交子空间持续学习),replay 是经验回放,两者都是持续学习方法;配上 CVEfixes,指向这个方向 |
| 数据集下载地址 | 下面标了 ✅ 的是我用 `git ls-remote` 实际验证过仓库存在;Zenodo / HuggingFace 的具体链接**我在沙箱里访问不到**,是凭记忆写的,你要自己核对 |

**如果推断错了,第 3、4 阶段的设计要跟着改。看到不对的地方直接说。**

---

## 1. 全景:总共要做什么

扩展版论文相对会议版,需要增加实质性的新内容。手上有两条扩展轴:

```
                 原数据集              CVEfixes
   原模型      [会议版已发表]        轴 B:换数据集
   Qwen        轴 A:换模型           (两轴同时换,不做)
```

**两轴各自单独换,不要同时换。** 同时换的话结果有差异你说不清是模型带来的还是数据带来的,审稿人一定会问,而且这个问题没法事后补救。

于是实验矩阵是三块:

| 块 | 内容 | 作用 |
|---|---|---|
| **R** 复现 | 7 方法 × 原数据 × 原模型 | 基线。证明 5090 上的管线是对的 |
| **A** 换模型 | 7 方法 × 原数据 × Qwen | 新贡献 1:方法是否跨模型成立 |
| **B** 换数据 | 7 方法 × CVEfixes × 原模型 | 新贡献 2:方法是否跨数据成立 |

加上显著性检验需要多种子,总运行数 = 3 块 × 7 方法 × N 种子。

**N=3 是显著性分析的下限,总共 63 次运行。** 这个数决定了成本,后面第 6 节细说。

---

## 2. 阶段划分与时间预算

### 阶段 0 — 恢复现场(半天)

必须先做完,后面全部依赖它。

1. **验证 5090 环境**
   ```bash
   cd /root/autodl-tmp && git clone https://github.com/Demonh0pe/ICISSP.git
   cd ICISSP && git checkout claude/autodl-experiment-migration-3ak8my
   bash env/setup.sh          # 看到 READY 才算过
   ```
   5090 是 sm_120,cu124 及更早的 torch wheel 能 import、`cuda.is_available()` 返回 True,**训练跑到一半才炸** `no kernel image is available`。自检就是把这个错误提前到第一分钟。

2. **找回 `train_swap.py` 或原 notebook**,推到本分支。这是后续所有代码工作的前提。

3. **确认原数据集在哪。** 如果原实验的数据文件还在,直接用;如果丢了,第 4 节有替代来源。

### 阶段 1 — 跑通一次,再谈规模(半天)

**先单方法、单种子、小步数跑通一次完整链路**,确认:
- 数据能加载
- 训练能跑完不 OOM
- 指标算出来的数量级跟会议版对得上

跑通之前不要开批量。批量跑一半发现指标算错,时间和钱一起没。

### 阶段 2 — 复现块 R(半天~一天)

7 个方法 × 3 种子跑基线。**这一块的意义是校验,不是新贡献**:如果 5090 上复现出的数字跟会议版差太远,说明迁移过程哪里出了问题,必须先查清楚再往下走。

差多少算"太远"?没有硬标准,但换硬件本身会带来小幅波动(cuDNN 算法选择、TF32 行为、batch 内浮点累加顺序都不同)。**方法之间的相对排序不变**比绝对数字对齐更重要,论文里也该按这个口径讲。

### 阶段 3 — 换模型块 A(一天)

`train_swap.py` 已经是干这个的。要注意:

- **模型规模直接决定你跑不跑得完**,见第 6 节
- 换模型后超参不一定还合适(学习率尤其),但**同一块内 7 个方法必须同卡、同超参、同种子集合**,否则方法间的比较无效
- 论文里注明硬件从 A100 换到 5090,以及"块内一致"这个原则

### 阶段 4 — 换数据集块 B(一天)

```bash
python data/prepare_cvefixes.py --db CVEfixes.db --out data/processed --languages all
```

预处理脚本已经写好并测试过了(`data/test_prepare_cvefixes.py`,22 项断言)。它做了两件防泄漏的事:

1. **全局去重,在切分之前。** CVEfixes 里同一段代码反复出现——vendored 副本、维护分支 backport、一个 commit 挂多个 CVE。不去重则同样代码同时进 train 和 test。
2. **按组切分,不按样本切。** 随机切样本会把同一方法的修复前/修复后版本分到两边:文本几乎一样、标签相反,这是最隐蔽的泄漏。

`stats.json` 会报告残余的 train/test 仓库重叠率。**这个数写进论文**,别假装是零。

### 阶段 5 — 统计、出图、回写论文(半天)

- 显著性检验:新分析用统一的标准口径。**会议版的 p=0.026 是已发表事实,不改。**
- f1 口径延续 sklearn binary f1。**在论文里明确写出用的是哪一种 F1**——binary / macro / weighted 数值差别很大,不写清楚是审稿人的常见质疑点。
- 稳定性分析:多种子的均值 ± 标准差。

---

## 3. 三天的现实排法

三天跑完 63 次运行 + 写论文,前提是阶段 0 和 1 顺利。不顺利就得砍。

**砍的优先级(从先砍起):**

1. 块 R 的种子数从 3 降到 1 —— 复现是校验,不需要显著性
2. 块 B 降级为"单种子 + 定性讨论"
3. 块 B 整块砍掉,如实写进 Future Work

**最后一条是底线:跑不成就如实写,不编数据。** 你有 `extended_paper.tex` 作保底稿,不至于交不出东西。一篇诚实标注"由于算力限制,跨数据集验证留待future work"的论文能过审;一篇编数据的过审了也是隐患。

---

## 4. 数据集去哪里找

### CVEfixes(你的方案 A)

✅ 仓库已验证存在,最新 tag **v1.0.8**:
```
https://github.com/secureIT-project/CVEfixes
```

**关键提醒:不要自己 build。** 仓库里的构建脚本要克隆几千个上游 repo、调 GitHub API、跑很久(以天计),还会因为 API 限流和 repo 消失而中途失败。

**去 README 里找 Zenodo 链接,下预构建的数据库 dump。** 我在沙箱里访问不到 Zenodo,没法把 DOI 抄给你,但 README 顶部就有。

下载注意:
- 解压后是 SQLite 库,**体积以 GB 计**
- AutoDL 上放 `/root/autodl-tmp/`(数据盘),**不要放系统盘**,系统盘会爆
- 下载慢的话开 AutoDL 学术加速

### 备选数据集(万一 CVEfixes 下不来或不合适)

以下仓库我都用 `git ls-remote` 验证过存在:

| 数据集 | 仓库 | 说明 |
|---|---|---|
| **BigVul** | ✅ `ZeoVan/MSR_20_Code_vulnerability_CSV_Dataset` | C/C++,来自 CVE 记录,函数级。经典基线,但已知有标签噪声 |
| **Devign** | ✅ `saikat107/Devign` | C,FFmpeg+QEMU,人工标注。也是 CodeXGLUE defect detection 那份 |
| **DiverseVul** | ✅ `wagner-group/diversevul` | C/C++,比 BigVul 大且做过去重 |
| **PrimeVul** | ✅ `DLVulDet/PrimeVul` | 2024,**专门针对前几个数据集的标签噪声和重复问题**做了清理,有配对版本 |
| **MegaVul** | ✅ `Icyrockton/MegaVul` | 较新,规模大 |
| **CleanVul** | ✅ `yikun-li/CleanVul` | 较新,主打标签质量 |
| **VulRepair** | ✅ `awsm-research/VulRepair` | 修复方向,不是检测 |

**如果目标是"换一个数据集验证方法的普适性",PrimeVul 其实比 CVEfixes 更值得考虑**:它是明确为了修正前人数据集缺陷而做的,用它做验证,审稿人对数据质量的质疑会少很多。而且它是现成的文件,不用像 CVEfixes 那样处理 SQLite。

不过这取决于你会议版用的是什么数据集——**新数据集要跟原数据集有足够差异**,才谈得上"跨数据集验证"。这个判断需要我知道原数据集是什么。

HuggingFace 上这些大多有镜像,但**具体的 repo id 我没法在这里验证,别照抄我写的**,自己去 HF 搜。

---

## 5. 模型去哪里找

Qwen 系列在 HuggingFace 和 ModelScope 都有。**AutoDL 在国内,ModelScope 通常比 HF 快得多:**

```bash
pip install modelscope
modelscope download --model Qwen/Qwen2.5-Coder-1.5B --local_dir /root/autodl-tmp/models/qwen
```

或者用 HF 镜像:
```bash
export HF_ENDPOINT=https://hf-mirror.com
```

模型也放 `/root/autodl-tmp/`,并且设 `HF_HOME` 指过去,否则默认缓存在系统盘,会爆。

---

## 6. 成本控制 —— 这一节决定你能不能跑完

**模型规模的选择比任何其他决定都更影响总成本。**

粗略量级(LoRA 微调,5090,单次运行):

| 模型规模 | 单次运行 | × 63 次 |
|---|---|---|
| ~1.5B | 二三十分钟 | 20-30 GPU 小时 |
| ~7B | 一两小时 | 60-120 GPU 小时 |

三天 = 72 小时,**还要扣掉调试、下载、写论文的时间**。7B 跑满整个矩阵在三天内不现实。

**建议用 1.5B 量级(如 Qwen2.5-Coder-1.5B)。** 理由不只是省钱:

- 论文要证明的是**方法之间的相对差异**,不是刷绝对 SOTA
- 小模型能让你跑满 3 种子 × 3 块,拿到真正的显著性和稳定性分析
- 大模型单种子跑出来的数字,统计上什么都说明不了

**跑得完的小模型完整矩阵 > 跑不完的大模型半张表。**

其他成本注意:

- **无人值守的长跑用 `nohup` 或 `tmux`**,SSH 断了训练不会死
- 训练完及时**关机**,AutoDL 按量计费是按开机时间算的,不是按 GPU 利用率
- **每跑完一个方法就存一次结果**,别等全部跑完再写盘——中途挂掉全白跑
- 盯余额。跑到一半停机比跑失败更麻烦(checkpoint 可能是半截的)

---

## 7. 论文里必须交代清楚的几件事

这些是审稿人一定会盯的地方,提前写好比事后补救省事:

1. **F1 的具体口径。** 用的是 sklearn 的 binary f1,不是 macro。明写。
2. **硬件变更。** A100 → 5090。同时说明:每个实验块内部保持同卡、同超参,块间不做绝对数值的直接比较。
3. **数据泄漏的处理。** 去重策略、切分粒度(按 CVE / repo / commit 分组)、以及残余的仓库重叠率——`stats.json` 里有现成的数。
4. **种子与波动。** 报告 N 个种子的均值 ± 标准差,不要只报单次最好结果。
5. **会议版 p=0.026** 是已发表事实,原样引用;新做的检验用统一的标准口径,并说明两者口径不同。
6. **没跑成的部分**如实写进 limitations / future work。

---

## 8. 当前仓库状态

```
env/setup.sh                   cu128 环境安装(5090 必须)
env/verify_env.py              8 项自检,强制真实 kernel,READY 才能开跑
data/prepare_cvefixes.py       CVEfixes.db → train/val/test.jsonl
data/test_prepare_cvefixes.py  合成库端到端测试,22 项断言,不需要真库
PLAN.md                        本文件
```

**还没做、且需要你的 `train_swap.py` 才能做的:**

- 补齐 7 个方法的统一训练入口(casr / hybrid / olora / replay 等)
- 结果汇总、显著性检验、出图
- 回写 `extended_paper.tex`

这三件都必须对齐你现有的数据 schema、指标调用方式和超参传法,猜出来的会让你返工。

---

## 9. 下一步

1. 改 AutoDL 密码(旧密码已泄漏在对话记录里)
2. 把 `train_swap.py`(或原 notebook)推到本分支 —— **解开我这边所有后续工作**
3. 在 5090 上跑 `bash env/setup.sh`,把输出贴回来
4. 同时开始下载 CVEfixes 的 Zenodo dump(体积大,早下早好)
