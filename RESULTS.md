# 实验结果与写作建议

全部数字来自 `runs/swap/metrics.csv`,可用以下命令复现:

```bash
python analysis/aggregate_runs.py --metrics runs/swap/metrics.csv \
  --out figures/ --metric f1_binary_pos1_LEGACY
```

指标口径:`f1_binary_pos1_LEGACY` 与会议版一致(sklearn binary F1,pos_label=1,
即 FIXED 类)。选它是为了让新表能与已发表的表并排;`macro_f1` 与
`f1_vulnerable` 同时记录在 CSV 里,随时可切换。

---

## 1. 实验配置

| 项 | 值 |
|---|---|
| 数据 | `data/splits_faithful`,43,773 样本 / 42 窗口 |
| 窗口 | 双月,2018–2024,前向链式(训练 W_t → 评测 W_{t+1}) |
| 骨干 | microsoft/phi-2 (2.7B)、Qwen2.5-Coder-1.5B |
| LoRA | r=16, α=32, dropout=0.05 |
| 训练 | 3 epoch, lr 2e-4, batch 32, **fp32** |
| 批次 | 按长度分组(`--group-by-length`) |
| 种子 | 核心对比 42/43/44;其余方法仅 42 |

**fp32 与分组批次是刻意固定的。** bf16 快 1.5 倍,但探针窗口偏移达 0.08,会把
精度变更混进架构对比;按长度分组快 1.95 倍,同样窗口只偏移 0.005,在噪声内。

---

## 2. 前向评测结果

| 骨干 | 方法 | 种子 | 均值 | 窗口 SD | 种子 SD | 种子范围 |
|---|---|---|---|---|---|---|
| phi-2 | window-only | 1 | **0.7236** | 0.0565 | — | — |
| Qwen | window-only | 3 | **0.6829** | 0.0556 | 0.0045 | 0.6780–0.6868 |
| Qwen | hybrid-casr | 3 | **0.6774** | 0.0532 | 0.0066 | 0.6732–0.6850 |
| Qwen | replay-3p | 1 | 0.6458 | 0.0518 | — | — |
| Qwen | olora | 1 | 0.5945 | 0.0424 | — | — |

配对 Wilcoxon(按 41 个窗口配对,种子平均后):

| 方法 vs 同骨干基线 | Δ | p |
|---|---|---|
| hybrid-casr @Qwen | −0.0056 | 0.032 |
| replay-3p @Qwen | −0.0371 | <0.0001 |
| olora @Qwen | −0.0884 | <0.0001 |

---

## 3. 反向保持(IBR)

| 骨干 | 方法 | @1 | @3 | @5 | @6 | 衰减 |
|---|---|---|---|---|---|---|
| phi-2 | window-only | 0.8991 | 0.8492 | 0.8263 | 0.8225 | 8.5% |
| Qwen | **hybrid-casr** | **0.9081** | **0.8123** | **0.7702** | **0.7552** | 16.8% |
| Qwen | window-only | 0.8799 | 0.7961 | 0.7646 | 0.7537 | 14.3% |
| Qwen | replay-3p | 0.8936 | 0.6446 | 0.6523 | 0.6400 | 28.4% |
| Qwen | olora | 0.6021 | 0.5934 | 0.5839 | 0.5935 | 1.4% |

---

## 3b. 缓冲区预算梯子

window-only 没有缓冲区,所以它 3 种子的那次跑天然就是"零预算"对照,
不需要额外重跑。加上 `hybrid-casr+b2x`(200+50)后是三个点:

| 预算 | 种子 | 前向 | IBR@1 | @3 | @5 | @6 |
|---|---|---|---|---|---|---|
| 0(window-only) | 3 | 0.6829 | 0.8799 | 0.7961 | 0.7646 | 0.7537 |
| 125(已发表) | 3 | 0.6774 | 0.9081 | 0.8123 | 0.7702 | 0.7552 |
| 250(b2x) | 1 | 0.6696 | 0.9514 | 0.8632 | 0.8106 | 0.8011 |

**五列全部单调**:前向严格递减,四个 lag 严格递增。

对零预算对照的配对检验:

| 预算 | Δ前向 | p | Δ IBR @1 / @3 / @5 / @6 | 平均 Δ IBR |
|---|---|---|---|---|
| 125 | −0.0056 | 0.031 | +0.028(3e-5) / +0.016(0.004) / +0.006(**0.46**) / +0.001(**0.74**) | +0.013 |
| 250 | −0.0133 | 0.030 | +0.071(2e-12) / +0.067(7e-12) / +0.046(1e-9) / +0.047(3e-10) | +0.058 |

两条要点:

1. **兑换率随预算变好。** 125 是 0.0056 换 0.013(2.3 倍),250 是 0.0133 换
   0.058(4.4 倍)。预算翻倍,前向代价翻倍,保持收益翻四倍。
2. **250 改变的是保持的性质,不只是幅度。** 125 的收益到 lag 5/6 已经不显著;
   250 的收益在四个 lag 上全部 p<1e-9,而且**不随 lag 衰减**。按论文自己的判据
   (回放效应应该在缓冲区覆盖范围外消退),这读作真的固化了,不是刚被回放过。

未跑:b4x(400+100)。三个点定方向,不定最优点。

---

## 4. 结论,按证据强度排序

### 4.1 前向优势不具跨架构稳健性 【核心,3 种子】

会议版在 phi-2 上报告 Hybrid-CASR 相对 window-only **+0.016**。
在 Qwen2.5-Coder-1.5B 上重跑,结果是 **−0.0056**。

**必须同时报告的两个事实:**

- 窗口配对检验显著(p=0.032)
- **但效应量小于 hybrid-casr 自身的种子间标准差(0.0066),两者种子范围重叠**
  (window-only 0.6780–0.6868,hybrid-casr 0.6732–0.6850)

所以正确的说法是**"优势未能复现 / 消失"**,不是"显著反转"。只报 p=0.032
而不报种子重叠,是选择性呈现。

**这一条本身就是方法论证据:** 单种子时该效应为 −0.0136(p=0.0022),
加到三种子后缩小到 −0.0056(p=0.032)。**效应量随种子数增加而减半**,
说明单次运行的结论在这个尺度上不可靠——包括会议版原本那个 +0.016。

### 4.2 反向保持是真实优势 【3 种子,方向一致】

Hybrid-CASR 在**全部四个 lag 上**均优于基线,且差距随时间距离单调衰减
(+0.028 → +0.016 → +0.006 → +0.002),与回放缓冲覆盖近期窗口的机制一致。

**综合 4.1 和 4.2:在新骨干上,Hybrid-CASR 用统计上可忽略的前向精度代价,
换取了明确的反向保持提升。** 这比会议版"两头都赢"的表述更可信,且落在论文
自己的 stability–plasticity 框架内。

### 4.3 两个"每窗口重建适配器"的方法在两个骨干上都垫底 【1 种子,但差距远超噪声】

`replay-3p`(−0.037)与 `olora`(−0.088)在 Qwen 上仍是最后两名,与会议版
在 phi-2 上的排序一致(0.622 / 0.599)。差距是种子噪声的 6–20 倍,单种子足够。

**`replay-3p` 的保持曲线提供了机制性证据:** 它前向倒数第二,IBR@1 却有 0.8936
(高于基线 0.8799,仅次于 hybrid-casr 的 0.9081);到 IBR@3 断崖下跌 0.25 至 0.6446。
它回放 t−1 与 t−2 两个窗口,
**保持曲线的断点恰好落在回放范围的边界上** —— 说明其"保持"来自回放的数据本身,
而非适配器中积累的参数。真正延续适配器的方法(window-only、hybrid-casr)
则平滑衰减。

这与 `AUDIT.md` 第 8 条相互印证:这两个方法的代码每窗口调用 `get_peft_model()`
重建适配器,不携带任何跨窗口参数。

### 4.4 更大的骨干保持得更好 【1 种子,谨慎表述】

phi-2 (2.7B) 的 IBR 衰减 8.5%,Qwen (1.5B) 为 14.3%。前向也高
(0.7236 vs 0.6829)。**但 phi-2 只有一个种子,且两模型的差异同时包含参数量、
预训练语料、架构三个变量**,不能归因为单纯的规模效应。作为观察报告,不作结论。

---

## 5. 建议写法

> 为检验会议版结论对骨干架构的依赖性,我们在 Qwen2.5-Coder-1.5B 上重跑了核心对比
> (3 个随机种子,41 个双月窗口,其余设置不变)。
>
> 前向性能上,会议版在 phi-2 报告的 Hybrid-CASR 优势(+0.016)未能复现:
> 新骨干上为 −0.0056。该差异在窗口配对的 Wilcoxon 检验下显著(p=0.032),
> 但小于 Hybrid-CASR 自身的种子间标准差(0.0066),且两方法的种子取值范围重叠
> (0.6732–0.6850 与 0.6780–0.6868)。我们因此认为该前向优势不具跨架构稳健性。
> 值得注意的是,若仅使用单一种子,同一对比给出 −0.0136(p=0.0022),
> 效应量在增加种子后减半——这提示此量级的差异需要多次运行才能可靠估计。
>
> 反向保持则呈现相反趋势:Hybrid-CASR 在全部四个 lag 上均优于 window-only
> (IBR@1 +0.028,@3 +0.016,@5 +0.006,@6 +0.002),差距随时间距离单调衰减,
> 与其回放缓冲覆盖近期窗口的设计一致。综合两方面,在新骨干上该方法以统计上
> 可忽略的前向代价换取了可测的保持提升。
>
> 方法排序的下半部分跨骨干保持稳定:Replay-3P(−0.037)与 OLoRA(−0.088)
> 在两个骨干上均显著劣于基线。Replay-3P 的保持曲线在 IBR@1 达到全场最高
> (0.894)后于 IBR@3 骤降至 0.645,断点与其回放窗口范围(t−1, t−2)重合,
> 表明其保持来自回放数据而非参数累积。

---

## 6. 必须写进 limitations 的

1. **数据为重建版本。** 原始 patch 集已不可得,依 NVD 记录与 GitHub 提交重新抓取,
   共 23,935 个 patch。与已发表数据的窗口计数相差 **−0.86%**(逐窗口中位 −0.66%,
   最大 4.6%),差异来自 414 个上游提交已被删除。以 `data/verify_dataset.py` 可核验。
2. **phi-2 基线未复现已发表数值。** 重建数据 + 新版依赖下为 0.7236,
   已发表为 0.651。可能来源:数据重建、transformers 4.35→5.15 与 peft 0.6→0.20
   的默认行为变更。未做拆分归因。**跨骨干对比在本文内部一致(同数据、同代码、
   同依赖、同设置),不受此影响。**
3. **仅核心对比有 3 种子。** phi-2 基线、replay-3p、olora 各 1 种子。
4. **未跑完的方法:** casr、replay-1p、lbcl、cumulative、zero-shot。算力所限。
5. **样本截断。** 20.6% 的样本超过 512 token 上限被截断(p90 = 1273,p99 ≈ 11000)。

---

## 7. 已写入论文

以上内容已写进 `extended_paper.tex`,分布在:

| 位置 | 内容 |
|---|---|
| `sec:replication`(方法) | 复现协议:第二骨干、重建语料、控制变量、种子 |
| `sec:cross-arch`(结果) | 两张新表 + 五段结论,对应本文件 §2–§4 |
| `sec:disc-interpretation` | 前向优势消失与 §4.1 的统计一致性如何调和 |
| `sec:disc-theory` | 滞后保持曲线区分"参数固化"与"刚回放过" |
| `sec:disc-limitations` | 本文件 §6 的五条,加上指标口径一条 |
| 摘要 / 结论 / `sec:differences` / RQ5 | 已按新实验重写 |

论文里的数字全部取自本文件,写作时按 `\textbf{}` 分段,便于逐条核对。

---

## 8. 图表文件

| 文件 | 用途 |
|---|---|
| `figures/fig5_seed_spread.pdf` | 种子范围与方法差距的对比,重叠可见 |
| `figures/table_multiseed.tex` | 均值 / 窗口 SD / 种子 SD |
| `figures/table_multiseed_significance.tex` | 配对检验 |
| `figures/ibr.csv` | 反向保持原始数值 |
| `figures/fig1`–`fig4` + `table_methods.tex` | 会议版数据按修正指标重出(见 `AUDIT.md`) |
